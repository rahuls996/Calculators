/** Lakhs — used for term premium formula (`computePrice` c6 income adj) and buy URL when income is not collected in UI. */
export const TERM_DEFAULT_INCOME_LAKHS = 50;

/** Max age (years) for term insurance (c6) — slider, state clamp, and `computePrice` c6. */
export const TERM_AGE_MAX = 55;
